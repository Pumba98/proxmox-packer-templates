proxmox_api_user: root@pam
proxmox_api_token_id: packer
proxmox_api_token_secret: <token>

# create a token via UI or
# pveum user token add root@pam packer --privsep 0

proxmox_api_host: pve.datacenter.internal:8006
proxmox_node: pve
proxmox_storage: local-zfs

# the defaults from the template
template_user: "root"
template_password: "opnsense"
# id of the packer template
template_tpl_vmid: 1273

# new password
opnsense_password: "secret"

ssh_key_file: ~/.ssh/id_ed25519
ssh_key_file_pub: ~/.ssh/id_ed25519.pub

# generate this with UI, create-opnsense-api-keys.sh or via create-opnsense-api-keys-playbook.yml
opnsense_key: dummy
opnsense_secret: dummy
